camera0: 0
camera1: 2
frame_width: 2360
frame_height: 720
# frame_width: 1344
# frame_height: 376
view_resize: 1
checkerboard_box_size_scale: 0.04
checkerboard_rows: 6
checkerboard_columns: 8
# Physical distance between the two camera centres in centimetres.
# Measure this on your rig and update it before running depth estimation.
baseline_cm: 12
